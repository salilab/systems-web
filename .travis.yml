language: python
dist: xenial
python:
  - 3.8
cache:
  pip: true
install:
  - pip install flask pytest-cov pytest-pep8 pyyaml
script:
  - py.test --cov=systems --cov-branch --pep8
after_success:
  - bash <(curl -s https://codecov.io/bash)
